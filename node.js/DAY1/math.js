function add(a,b){
    return a+b;
}
module.exports = add;
// when we will run this then we will get a function.(o/p like this:math value is  [Function: add])
/*function sub(a,b){
    return a-b;
}*/

//module.exports=sub; // when we will run this we will get that it will overwrite and gives us sub function.

/*module.exports = {
    add,
    sub,
}*/ // If we will write like this and we will run then we will get the output.